# Recursive variable. This will print "later" below
one = 1-${later_variable}
# Simply expanded variable. This will not print "later" below
two := 2-${later_variable}

later_variable = later

all: 
	@echo $(one)
	@echo $(two)
